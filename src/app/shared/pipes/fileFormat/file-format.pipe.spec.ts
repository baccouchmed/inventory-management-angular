import { FileFormatPipe } from './file-format.pipe';

describe('StatusFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FileFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
